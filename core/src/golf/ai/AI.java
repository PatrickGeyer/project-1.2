
/*package golf.course;
import golf.physics.*;
import golf.course.object.*;
*/
public class AI {
     /* public void calculate_shot(course, ball){
                //calculate the direction & strength needed
                x=
                y=
                return Vector2d(x, y);  //returns the direction and strength needed for the shot
                                        //then proceed with the same actions you would have when playing golf manually
        }
        
        public void calculate_gradient(course, ball) {
          p=
          delta=
          return Vector2d gradient(p, delta);
      
   }
   }
*/
}
